Config = {}

-- Translation for the UI.
Config.LANGUAGE = {
  HUD_EDITOR_DISCLAIMER = 'Enter a number at which the element should not be shown.',
  NUMBER_IN_PERCENTAGES = 'Number in percentages, 0 -> 100',
  HUD_CONTROL = 'HUD control',
  CLOSE = 'Close',
  SAVE = 'Save',
}

-- Custom constants for the UI.
Config.MULTIPLIERS = {
  WAVE_SPEED = 0.6,
}
